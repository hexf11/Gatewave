# 更新日志

本项目按照语义化版本管理。

## 0.2.3 - 2026-08-04

### TCP 热路径轻量化

- TCP 空闲时间轮仍以每次 I/O 的真实活动时间判断过期，但同一连接最多每秒重排一次槽位，避免持续传输反复修改时间轮集合。
- 上传/下载计数改为每 256 KiB、每秒或连接关闭时精确批量上报，减少每次 socket 写入触发的跨线程原子操作。
- DirectBuffer 缓存改为 Selector lane 单线程持有的分级队列；转发指标使用 lane 内普通计数并每秒发布只读快照。
- 每个 Selector 就绪事件只读取一次单调时钟，排队、重调度、活动时间和流量批次共享该时间戳。
- 单 LAN 客户端直接使用轮次预算，不进入多客户端公平 HashMap；多客户端等额份额及交互/大流隔离语义保持不变。
- 避免重复写入未变化的 `SelectionKey.interestOps`，并对微秒延迟直方图进行固定间隔采样。

### 验证

- 46 项单元测试全部通过，Release Lint、R8 资源压缩、APK 与 AAB 构建通过。
- Pixel 10a 与 v0.2.2 在相同 Wi-Fi、相同 Google VPN 出口完成 4 轮交错 A/B；两版各 160/160 个网页尺寸请求成功。
- 该测试窗口中，小请求 P95 从 4.030 秒降至 2.972 秒，混合负载 P95 从 3.737 秒降至 3.279 秒，后台有效吞吐中位数从 3.691 Mbps 提升至 4.939 Mbps。
- 公网 VPN 路径存在明显波动，上述数据只作为本次同出口回归门槛，不代表固定带宽或延迟承诺。

## 0.2.2 - 2026-08-03

### 网页交互调度

- TCP Relay 根据活跃连接数动态调整单连接事件预算；每个方向前 256 KiB 识别为交互流量，持续大流在高连接数时更早让出 Selector。
- 每个 Selector 轮次为交互流和大流分别预留字节池，防止遍历顺序让大型下载占用网页小请求的预算。
- 同一 lane 的多个 LAN 客户端按流量类别获得等额字节份额，未使用容量在下一轮立即回收；单客户端仍可持续使用全部链路。
- 增加 TCP 交互直写量、公平让出次数、缓冲排队 P50/P95 和预算重调度 P50/P95 指标，并贯通活动页与诊断报告。
- 新增可重复的混合负载工具，同时测量空载小请求、后台大下载下的小请求和后台有效吞吐。

### 验证

- 调度策略单元测试覆盖 256 KiB 分类边界、动态预算、交互/大流隔离、双客户端等额份额和未用预算退还。
- Pixel 10a 的多轮混合负载中，每轮 30/30 小请求成功，后台大流保持活动；Gatewave 内部缓冲排队 P95 为 2.5 ms，预算重调度 P95 为 2.5 ms，结束后排队回到 0。
- 80 路并发网页尺寸请求连续执行两轮均 80/80 成功，应用记录峰值 100 会话，结束后数据面排队为 0。
- 公网小请求 P95 随 VPN 路径在 4.10–6.58 秒间波动，因此只作为回归记录，不声明固定公网延迟增幅。

## 0.2.1 - 2026-08-03

### 高延迟链路优化

- 上游 TCP 接收/发送窗口按性能模式调优；极速模式请求 8 MiB/2 MiB，减少高 RTT 路径中过小 socket 窗口的限制。
- TCP Relay 在同一个 Selector 就绪事件内完成读后直写，并以 64–512 KiB 有界预算连续排空，减少数据块之间的事件循环停顿且保留多会话公平性。
- 极速模式自适应 DirectBuffer 上限提升至 128 KiB、每 lane 空闲池提升至 8 MiB；增加排队、直写、背压、公平让出、复用和分配指标。
- SOCKS 控制通道预读的首批 TCP payload 会完整交接给数据面，避免握手与业务数据合并发送时丢失首包。
- UDP 增加字面量地址与目标缓存快路径，减少重复解析、Selector 投递和临时对象分配，并公开解析、队列与丢弃指标。
- 网络诊断增加 VPN TCP RTT、单流和 4 流短下载，在高延迟且并发收益明显时标记单流受限。

### 验证

- Pixel 10a 同一测试窗口的 5 MiB 对照中，单流由 4.82 Mbps 提升至 5.36 Mbps，4 流聚合由 14.85 Mbps 提升至 19.53 Mbps；公网路径存在波动，该结果用于确认未回退而非承诺固定增幅。
- 16 路并发短下载全部返回 HTTP 200，聚合 15.66 Mbps；完成后 TCP 数据面排队归零，未发现 Relay 崩溃或异常退出。

## 0.2.0 - 2026-08-03

### 高并发数据面

- Pixel 级设备的动态容量提升至 1024 会话、1024 accept backlog 与 128 个 UDP 关联；较小内存或低 FD 设备自动降级。
- UDP 转发迁移到共享非阻塞 DatagramChannel Selector，不再为每个 UDP 关联占用工作线程。
- 多客户端准入采用动态公平份额；满载时为后到设备回收占用过量的最旧会话，同时保留单设备使用全部空闲容量的能力。
- TCP 空闲回收改为 O(1) 时间轮，增加 8–64 KiB 自适应 DirectBuffer、缓冲池上限、系统内存回收、读写背压和 15 秒半关闭排空窗口。
- 域名建连增加 250 ms Happy Eyeballs 地址竞速与按 VPN 网络隔离的地址健康排序。

### 性能与可观测性

- 新增均衡、极速、省电三档性能模式；极速模式持有 Wi-Fi 低延迟锁，严重温控状态会自动释放并清理缓冲池。
- 增加容量、峰值、活跃设备、DNS 缓存、建连 P50/P95、实时吞吐、UDP 丢弃、公平回收、Selector 和缓冲池指标。
- 网络诊断报告加入数据面指标，并隐藏 LAN 地址、VPN 标识与出口 IP，便于匿名分享。
- 稳定统计会在节流窗口结束后静默补刷，避免最终活跃数在磁盘快照中停留为旧值。

### 真机验证

- Pixel 10a 上 1024/1024 路 SOCKS5 握手保持成功，峰值线程 37 条。
- 500/500 路 TCP 突发建连成功；128/128 个 UDP 关联同时保持成功。
- HTTP/3 QUIC 握手、20 MiB 息屏传输、Wi-Fi 断开/恢复、温控降级/恢复与满载第二客户端接入通过。

## 0.1.2 - 2026-08-03

### 性能优化

- 为 VPN 域名解析增加 60 秒正缓存、5 秒负缓存和同域名并发请求合并；VPN 切换后立即清空。
- SOCKS5 greeting、请求解析、DNS 回调、上游 `connect()` 和 UDP 控制通道迁移到共享非阻塞状态机。
- TCP Selector lane 根据 CPU 核心数动态选择 2–4 条，握手 Selector 在高性能设备上使用 2 条。
- TCP DirectBuffer 改为按需获取并按 lane 复用，空闲连接不再预分配两块 32 KiB 缓冲区。
- 活跃 TCP/UDP 数量改为原子计数，移除每秒遍历全部会话的统计开销。
- 状态先在内存中发布，每 5 秒异步持久化；局域网地址只在网络变化时刷新，通知只在显示内容变化时更新。

### 验证

- Pixel 10a 上 120 路并发 SOCKS5 TCP 建连全部成功，应用线程峰值为 41 条。
- SOCKS5 TCP VPN 出口、UDP DNS 和 10 MiB 连续转发均通过真机检查。

## 0.1.1 - 2026-08-03

### 性能优化

- TCP 转发由每连接双阻塞线程改为双 Selector 共享 NIO 转发。
- SOCKS5 建连线程改为 8–128 条弹性线程，突发建连完成后自动回收。
- 服务端等待队列从 64 增加到 256，改善浏览器集中建连体验。
- UDP 转发使用独立、有界的工作线程，避免影响 TCP 页面加载。

## 0.1.0 - 2026-08-03

### 新增

- 局域网 SOCKS5 TCP 与 UDP 代理。
- 系统 VPN 绑定与 VPN 中断防泄漏处理。
- Clash YAML 订阅、分享和二维码。
- 代理端口、订阅端口、UDP、启动行为和主题设置。
- 网络链路一键自检与报告复制、分享。
- Material 3 界面、深浅色主题和 Android 16 适配。
- Gatewave 自适应、圆形和主题化应用图标。
- GitHub Actions 持续集成和签名发布流程。
